! $Id: format,v 1.1 2001/12/07 16:54:12 toast Exp $
FORMAT(width text)
  Returns the text, formatted with padding on the left to fill
  width characters.

Example:
  format(15 What's up?)			- Returns "     What's up?"

See also:
  FUNCTIONS CENTER
  FUNCTIONS LFORMAT
