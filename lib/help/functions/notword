! $Id: notword,v 1.1 2001/12/07 16:54:16 toast Exp $
NOTWORD(index wordlist)
  Returns all but the index-th word in wordlist.  The first word
  in wordlist has index 0.

Examples:
  notword(0 see jack run)		- Returns "jack run"
  notword(1 see jack run)		- Returns "see run"
  notword(2 see jack run)		- Returns "see jack"

See also:
  FUNCTIONS STRCNT
  FUNCTIONS WORD
